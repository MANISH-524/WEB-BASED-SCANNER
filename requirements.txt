# OBSIDIAN — Autonomous Web & API Security Framework
# Install with: pip install -r requirements.txt

# Core HTTP (required — bootstrap auto-installs if missing)
requests>=2.28.0
urllib3>=1.26.0

# Async core (recommended — enables the high-throughput crawler/prober).
# If absent, OBSIDIAN transparently uses a threaded sync fallback.
httpx>=0.27.0

# Banner art generation helper (optional; pre-baked art ships in-repo)
# pyfiglet>=1.0.0

# External CLI scanners are auto-installed via pip/apt/go/gem as needed
# (nuclei, subfinder, httpx-cli, ffuf, sqlmap, dalfox, nikto, sslyze, …).
# Optional extra integrations OBSIDIAN will use if present on PATH:
#   testssl.sh, retire (retire.js), gitleaks, wapiti, jaeles, dontgo403
